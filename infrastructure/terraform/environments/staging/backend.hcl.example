# Copy to backend.hcl (gitignored) and fill in your account values (ADR-0088).
# terraform init -backend-config=backend.hcl
bucket         = "<your-org>-terraform-state"
region         = "us-east-1"
dynamodb_table = "terraform-state-lock"
